package com.itemcarts.haha.ui

import net.runelite.client.ui.PluginPanel
import javax.inject.Singleton

@Singleton
class ItemCartsPluginPanel : PluginPanel() {
  init {
    // layout = BorderLayout()
    // scrollPane.background = ColorScheme.DARK_GRAY_COLOR
    // scrollPane.verticalScrollBar.preferredSize = Dimension(8, 0)
    // scrollPane.verticalScrollBar.background = ColorScheme.DARK_GRAY_COLOR
    // scrollPane.verticalScrollBar.foreground = ColorScheme.LIGHT_GRAY_COLOR
  }
}